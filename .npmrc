# One lockfile, no phantom dependencies: a package may only import what it declares.
auto-install-peers=false
strict-peer-dependencies=false
# Reproducible installs in CI.
prefer-frozen-lockfile=true
